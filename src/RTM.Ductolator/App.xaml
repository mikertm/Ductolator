<Application
    x:Class="RTM.Ductolator.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary>
                    <!--  Tokyo Daylight Palette - Warm sunrise tones  -->
                    <Color x:Key="Color.Primary.Light">#D97706</Color>
                    <Color x:Key="Color.PrimaryDark.Light">#B45309</Color>
                    <Color x:Key="Color.Secondary.Light">#E11D48</Color>
                    <Color x:Key="Color.Surface.Light">#FFFBF5</Color>
                    <Color x:Key="Color.SurfaceAlt.Light">#FEF7ED</Color>
                    <Color x:Key="Color.Card.Light">#FFFAF3</Color>
                    <Color x:Key="Color.CardAlt.Light">#FFF5E6</Color>
                    <Color x:Key="Color.Emphasis.Light">#FEF3C7</Color>
                    <Color x:Key="Color.Success.Light">#ECFDF5</Color>
                    <Color x:Key="Color.Warning.Light">#FEF3C7</Color>
                    <Color x:Key="Color.Muted.Light">#F5F5F4</Color>
                    <Color x:Key="Color.Border.Light">#E7E5E4</Color>
                    <Color x:Key="Color.BorderAlt.Light">#FBBF24</Color>
                    <Color x:Key="Color.BorderMuted.Light">#D6D3D1</Color>
                    <Color x:Key="Color.Text.Primary.Light">#1C1917</Color>
                    <Color x:Key="Color.Text.Subtle.Light">#57534E</Color>
                    <Color x:Key="Color.Text.Danger.Light">#B91C1C</Color>

                    <!--  Tokyo Night Storm Palette - Official VS Code theme colors  -->
                    <Color x:Key="Color.Primary.Dark">#7aa2f7</Color>
                    <Color x:Key="Color.PrimaryDark.Dark">#5d7ec4</Color>
                    <Color x:Key="Color.Secondary.Dark">#bb9af7</Color>
                    <Color x:Key="Color.Surface.Dark">#1a1b26</Color>
                    <Color x:Key="Color.SurfaceAlt.Dark">#24283b</Color>
                    <Color x:Key="Color.Card.Dark">#24283b</Color>
                    <Color x:Key="Color.CardAlt.Dark">#414868</Color>
                    <Color x:Key="Color.Emphasis.Dark">#414868</Color>
                    <Color x:Key="Color.Success.Dark">#1a2e1a</Color>
                    <Color x:Key="Color.Warning.Dark">#2e2a1a</Color>
                    <Color x:Key="Color.Muted.Dark">#24283b</Color>
                    <Color x:Key="Color.Border.Dark">#414868</Color>
                    <Color x:Key="Color.BorderAlt.Dark">#7aa2f7</Color>
                    <Color x:Key="Color.BorderMuted.Dark">#565f89</Color>
                    <Color x:Key="Color.Text.Primary.Dark">#c0caf5</Color>
                    <Color x:Key="Color.Text.Subtle.Dark">#9aa5ce</Color>
                    <Color x:Key="Color.Text.Danger.Dark">#f7768e</Color>

                    <!--  Brush aliases using DARK palette - Tokyo Midnight  -->
                    <SolidColorBrush x:Key="Brush.Primary" Color="{StaticResource Color.Primary.Dark}" />
                    <SolidColorBrush x:Key="Brush.PrimaryDark" Color="{StaticResource Color.PrimaryDark.Dark}" />
                    <SolidColorBrush x:Key="Brush.Secondary" Color="{StaticResource Color.Secondary.Dark}" />
                    <SolidColorBrush x:Key="Brush.Accent" Color="{StaticResource Color.Primary.Dark}" />
                    <SolidColorBrush x:Key="Brush.InputBackground" Color="#24283b" />
                    <SolidColorBrush x:Key="Brush.AppBackground" Color="{StaticResource Color.Surface.Dark}" />
                    <SolidColorBrush x:Key="Brush.Surface" Color="{StaticResource Color.Surface.Dark}" />
                    <SolidColorBrush x:Key="Brush.SurfaceAlt" Color="{StaticResource Color.SurfaceAlt.Dark}" />
                    <SolidColorBrush x:Key="Brush.CardBackground" Color="{StaticResource Color.Card.Dark}" />
                    <SolidColorBrush x:Key="Brush.CardAltBackground" Color="{StaticResource Color.CardAlt.Dark}" />
                    <SolidColorBrush x:Key="Brush.EmphasisBackground" Color="{StaticResource Color.Emphasis.Dark}" />
                    <SolidColorBrush x:Key="Brush.SuccessBackground" Color="{StaticResource Color.Success.Dark}" />
                    <SolidColorBrush x:Key="Brush.WarningBackground" Color="{StaticResource Color.Warning.Dark}" />
                    <SolidColorBrush x:Key="Brush.MutedBackground" Color="{StaticResource Color.Muted.Dark}" />
                    <SolidColorBrush x:Key="Brush.Border" Color="{StaticResource Color.Border.Dark}" />
                    <SolidColorBrush x:Key="Brush.BorderAlt" Color="{StaticResource Color.BorderAlt.Dark}" />
                    <SolidColorBrush x:Key="Brush.BorderMuted" Color="{StaticResource Color.BorderMuted.Dark}" />
                    <SolidColorBrush x:Key="Brush.TextPrimary" Color="{StaticResource Color.Text.Primary.Dark}" />
                    <SolidColorBrush x:Key="Brush.TextSubtle" Color="{StaticResource Color.Text.Subtle.Dark}" />
                    <SolidColorBrush x:Key="Brush.TextDanger" Color="{StaticResource Color.Text.Danger.Dark}" />
                    <SolidColorBrush x:Key="Brush.TextSuccess" Color="#9ece6a" />
                    <SolidColorBrush x:Key="Brush.TextWarning" Color="#e0af68" />
                    <SolidColorBrush x:Key="Brush.TextInfo" Color="#7dcfff" />
                    <SolidColorBrush x:Key="Brush.TextMutedStrong" Color="#a9b1d6" />
                    <SolidColorBrush x:Key="Brush.TextDangerSoft" Color="#f7768e" />
                    <SolidColorBrush x:Key="Brush.ChipSuccessBackground" Color="#1a2e1a" />
                    <SolidColorBrush x:Key="Brush.ChipSuccessBorder" Color="#9ece6a" />
                    <SolidColorBrush x:Key="Brush.ChipWarningBackground" Color="#2e2a1a" />
                    <SolidColorBrush x:Key="Brush.ChipWarningBorder" Color="#e0af68" />
                    <SolidColorBrush x:Key="Brush.ChipInfoBackground" Color="#1a2a3b" />
                    <SolidColorBrush x:Key="Brush.ChipInfoBorder" Color="#7dcfff" />
                    <SolidColorBrush x:Key="Brush.ChipDangerBackground" Color="#2e1a1a" />
                    <SolidColorBrush x:Key="Brush.ChipDangerBorder" Color="#f7768e" />
                    <SolidColorBrush x:Key="Brush.ChipPurpleBackground" Color="#2a1a3b" />
                    <SolidColorBrush x:Key="Brush.ChipPurpleBorder" Color="#bb9af7" />

                    <!--  Typography  -->
                    <Style TargetType="TextBlock">
                        <Setter Property="FontFamily" Value="Segoe UI" />
                        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
                        <Setter Property="TextOptions.TextFormattingMode" Value="Ideal" />
                        <Setter Property="TextOptions.TextRenderingMode" Value="Auto" />
                    </Style>

                    <Style TargetType="TextBox">
                        <Setter Property="FontFamily" Value="Segoe UI" />
                        <Setter Property="Padding" Value="8,6" />
                        <Setter Property="MinHeight" Value="32" />
                        <Setter Property="VerticalContentAlignment" Value="Center" />
                        <Setter Property="SnapsToDevicePixels" Value="True" />
                        <Setter Property="Background" Value="{StaticResource Brush.InputBackground}" />
                        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
                        <Setter Property="CaretBrush" Value="{StaticResource Brush.TextPrimary}" />
                        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
                    </Style>

                    <!--  ComboBox dark theme  -->
                    <Style TargetType="ComboBox">
                        <Setter Property="Background" Value="{StaticResource Brush.InputBackground}" />
                        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
                        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
                        <Setter Property="BorderThickness" Value="1" />
                        <Setter Property="Padding" Value="8,6" />
                        <Setter Property="MinHeight" Value="32" />
                    </Style>

                    <!--  ListBox dark theme  -->
                    <Style TargetType="ListBox">
                        <Setter Property="Background" Value="{StaticResource Brush.InputBackground}" />
                        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
                        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
                    </Style>

                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
                        <Setter Property="Padding" Value="8,4" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource Brush.CardAltBackground}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{StaticResource Brush.Primary}" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                    <!--  CheckBox dark theme  -->
                    <Style TargetType="CheckBox">
                        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
                    </Style>

                    <!--  Borders  -->
                    <Style x:Key="CardBorderStyle" TargetType="Border">
                        <Setter Property="Background" Value="{StaticResource Brush.CardBackground}" />
                        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
                        <Setter Property="BorderThickness" Value="1" />
                        <Setter Property="CornerRadius" Value="10" />
                        <Setter Property="Padding" Value="8" />
                        <Setter Property="Margin" Value="0,0,0,12" />
                    </Style>

                    <Style
                        x:Key="AccentBorderStyle"
                        BasedOn="{StaticResource CardBorderStyle}"
                        TargetType="Border">
                        <Setter Property="Background" Value="{StaticResource Brush.EmphasisBackground}" />
                        <Setter Property="BorderBrush" Value="{StaticResource Brush.BorderAlt}" />
                    </Style>

                    <Style
                        x:Key="MutedBorderStyle"
                        BasedOn="{StaticResource CardBorderStyle}"
                        TargetType="Border">
                        <Setter Property="Background" Value="{StaticResource Brush.MutedBackground}" />
                        <Setter Property="BorderBrush" Value="{StaticResource Brush.BorderMuted}" />
                    </Style>

                    <Style
                        x:Key="SuccessBorderStyle"
                        BasedOn="{StaticResource CardBorderStyle}"
                        TargetType="Border">
                        <Setter Property="Background" Value="{StaticResource Brush.SuccessBackground}" />
                        <Setter Property="BorderBrush" Value="#9ece6a" />
                    </Style>

                    <Style
                        x:Key="WarningBorderStyle"
                        BasedOn="{StaticResource CardBorderStyle}"
                        TargetType="Border">
                        <Setter Property="Background" Value="{StaticResource Brush.WarningBackground}" />
                        <Setter Property="BorderBrush" Value="#e0af68" />
                    </Style>

                    <Style
                        x:Key="HighlightBorderStyle"
                        BasedOn="{StaticResource CardBorderStyle}"
                        TargetType="Border">
                        <Setter Property="Background" Value="{StaticResource Brush.CardAltBackground}" />
                        <Setter Property="BorderBrush" Value="{StaticResource Brush.BorderAlt}" />
                    </Style>

                    <Style x:Key="BadgeBorderStyle" TargetType="Border">
                        <Setter Property="Background" Value="{StaticResource Brush.CardBackground}" />
                        <Setter Property="BorderBrush" Value="{StaticResource Brush.BorderMuted}" />
                        <Setter Property="BorderThickness" Value="1" />
                        <Setter Property="CornerRadius" Value="10" />
                        <Setter Property="Padding" Value="8,3" />
                        <Setter Property="Margin" Value="0,6,6,0" />
                    </Style>

                    <!--  Buttons  -->
                    <Style x:Key="BaseButtonStyle" TargetType="Button">
                        <Setter Property="Background" Value="{StaticResource Brush.EmphasisBackground}" />
                        <Setter Property="BorderBrush" Value="{StaticResource Brush.BorderAlt}" />
                        <Setter Property="BorderThickness" Value="1" />
                        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
                        <Setter Property="Padding" Value="12,6" />
                        <Setter Property="FontWeight" Value="SemiBold" />
                        <Setter Property="Cursor" Value="Hand" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border
                                        x:Name="border"
                                        Padding="{TemplateBinding Padding}"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="{StaticResource Brush.CardAltBackground}" />
                                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource Brush.Border}" />
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="{StaticResource Brush.EmphasisBackground}" />
                                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource Brush.Primary}" />
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter TargetName="border" Property="Opacity" Value="0.6" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <Style BasedOn="{StaticResource BaseButtonStyle}" TargetType="Button" />

                    <Style
                        x:Key="PrimaryButtonStyle"
                        BasedOn="{StaticResource BaseButtonStyle}"
                        TargetType="Button">
                        <Setter Property="Background" Value="{StaticResource Brush.Primary}" />
                        <Setter Property="Foreground" Value="White" />
                        <Setter Property="BorderBrush" Value="{StaticResource Brush.PrimaryDark}" />
                        <Setter Property="Padding" Value="14,7" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border
                                        x:Name="border"
                                        Padding="{TemplateBinding Padding}"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="{StaticResource Brush.PrimaryDark}" />
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="{StaticResource Brush.Secondary}" />
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter TargetName="border" Property="Opacity" Value="0.65" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <Style
                        x:Key="PillButtonStyle"
                        BasedOn="{StaticResource BaseButtonStyle}"
                        TargetType="Button">
                        <Setter Property="Padding" Value="14,6" />
                        <Setter Property="Background" Value="{StaticResource Brush.EmphasisBackground}" />
                        <Setter Property="BorderBrush" Value="{StaticResource Brush.BorderAlt}" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border
                                        x:Name="border"
                                        Padding="{TemplateBinding Padding}"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="16">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="{StaticResource Brush.CardAltBackground}" />
                                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource Brush.Primary}" />
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="{StaticResource Brush.EmphasisBackground}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <!--  Tab Control  -->
                    <Style TargetType="TabControl">
                        <Setter Property="Background" Value="{StaticResource Brush.AppBackground}" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Padding" Value="0,12,0,0" />
                    </Style>

                    <Style TargetType="TabItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border
                                        x:Name="Border"
                                        Margin="0,0,8,0"
                                        Padding="16,10"
                                        Background="Transparent"
                                        BorderBrush="Transparent"
                                        BorderThickness="0,0,0,3"
                                        CornerRadius="4,4,0,0">
                                        <ContentPresenter
                                            x:Name="ContentSite"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            ContentSource="Header" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource Brush.Accent}" />
                                            <Setter TargetName="Border" Property="Background" Value="{StaticResource Brush.SurfaceAlt}" />
                                            <Setter Property="Foreground" Value="{StaticResource Brush.Accent}" />
                                            <Setter Property="FontWeight" Value="SemiBold" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="False">
                                            <Setter Property="Foreground" Value="{StaticResource Brush.TextSubtle}" />
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="{StaticResource Brush.CardAltBackground}" />
                                            <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
                                            <Setter Property="Cursor" Value="Hand" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <!--  ScrollBars & ScrollViewer  -->
                    <Style TargetType="ScrollBar">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Foreground" Value="{StaticResource Brush.BorderMuted}" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ScrollBar">
                                    <Grid
                                        x:Name="GridRoot"
                                        Width="10"
                                        Background="{TemplateBinding Background}">
                                        <Track
                                            x:Name="PART_Track"
                                            Focusable="false"
                                            IsDirectionReversed="true">
                                            <Track.Thumb>
                                                <Thumb x:Name="Thumb" Background="{TemplateBinding Foreground}" />
                                            </Track.Thumb>
                                            <Track.DecreaseRepeatButton>
                                                <RepeatButton
                                                    Command="ScrollBar.LineUpCommand"
                                                    Focusable="false"
                                                    Opacity="0" />
                                            </Track.DecreaseRepeatButton>
                                            <Track.IncreaseRepeatButton>
                                                <RepeatButton
                                                    Command="ScrollBar.LineDownCommand"
                                                    Focusable="false"
                                                    Opacity="0" />
                                            </Track.IncreaseRepeatButton>
                                        </Track>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="Orientation" Value="Horizontal">
                                            <Setter TargetName="GridRoot" Property="Width" Value="Auto" />
                                            <Setter TargetName="GridRoot" Property="Height" Value="10" />
                                            <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <Style TargetType="Thumb">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Thumb">
                                    <Border
                                        Margin="1"
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="5" />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <Style TargetType="ScrollViewer">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ScrollViewer">
                                    <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <ScrollContentPresenter
                                            x:Name="PART_ScrollContentPresenter"
                                            Grid.Row="0"
                                            Grid.Column="0"
                                            Margin="{TemplateBinding Padding}"
                                            CanContentScroll="{TemplateBinding CanContentScroll}"
                                            CanHorizontallyScroll="False"
                                            CanVerticallyScroll="False"
                                            Content="{TemplateBinding Content}"
                                            ContentTemplate="{TemplateBinding ContentTemplate}" />
                                        <ScrollBar
                                            x:Name="PART_VerticalScrollBar"
                                            Grid.Row="0"
                                            Grid.Column="1"
                                            AutomationProperties.AutomationId="VerticalScrollBar"
                                            Cursor="Arrow"
                                            Maximum="{TemplateBinding ScrollableHeight}"
                                            Minimum="0"
                                            ViewportSize="{TemplateBinding ViewportHeight}"
                                            Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                            Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" />
                                        <ScrollBar
                                            x:Name="PART_HorizontalScrollBar"
                                            Grid.Row="1"
                                            Grid.Column="0"
                                            AutomationProperties.AutomationId="HorizontalScrollBar"
                                            Cursor="Arrow"
                                            Maximum="{TemplateBinding ScrollableWidth}"
                                            Minimum="0"
                                            Orientation="Horizontal"
                                            ViewportSize="{TemplateBinding ViewportWidth}"
                                            Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                                            Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" />
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <!--  DataGrid  -->
                    <Style TargetType="DataGrid">
                        <Setter Property="Background" Value="{StaticResource Brush.CardBackground}" />
                        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="RowBackground" Value="{StaticResource Brush.CardBackground}" />
                        <Setter Property="AlternatingRowBackground" Value="{StaticResource Brush.CardAltBackground}" />
                        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource Brush.BorderMuted}" />
                        <Setter Property="VerticalGridLinesBrush" Value="{StaticResource Brush.BorderMuted}" />
                        <Setter Property="HeadersVisibility" Value="Column" />
                        <Setter Property="AutoGenerateColumns" Value="False" />
                    </Style>

                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{StaticResource Brush.CardAltBackground}" />
                        <Setter Property="Foreground" Value="{StaticResource Brush.TextSubtle}" />
                        <Setter Property="Padding" Value="10,8" />
                        <Setter Property="FontWeight" Value="SemiBold" />
                        <Setter Property="BorderThickness" Value="0,0,0,1" />
                        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
                    </Style>

                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="8,6" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{StaticResource Brush.EmphasisBackground}" />
                                <Setter Property="Foreground" Value="{StaticResource Brush.Primary}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource Brush.EmphasisBackground}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                    <Style
                        x:Key="ChipBorderStyle"
                        BasedOn="{StaticResource BadgeBorderStyle}"
                        TargetType="Border" />
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
